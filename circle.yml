machine:
  node:
    version: 0.11.8
    
test:
  override:
    - mocha
  post:
    - echo "$CIRCLE_ARTIFACTS"
    
    
deployment:
  dockerhub:
     branch: master
     commands:
         - mkdir ${CIRCLE_ARTIFACTS}/nodemod
         - cp -R ./* ${CIRCLE_ARTIFACTS}/nodemod/
#        - $DOCKERTRIGGER

   post:
       - $ls 
       - $ls $CIRCLE_ARTIFACTS

