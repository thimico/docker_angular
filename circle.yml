machine:
  node:
    version: 0.11.8
    
test:
  override:
    - mocha
  post:
    - echo "$CIRCLE_ARTIFACTS"
    
    
deployment:
  dockerhub:
     branch: master
     commands:
         - mkdir ${CIRCLE_ARTIFACTS}/nodemod
         - cp -R ./node_modules ${CIRCLE_ARTIFACTS}/nodemod/
#        - $DOCKERTRIGGR

         - ls $CIRCLE_ARTIFACTS

